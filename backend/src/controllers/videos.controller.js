/**
 * Videos: paywalled playback + progress.
 * Implementation will mint short-lived signed URLs from the chosen provider.
 */
exports.getPlaybackUrl = async (_req, res) => res.status(501).json({ error: 'TODO: getPlaybackUrl' });
exports.saveProgress   = async (_req, res) => res.status(501).json({ error: 'TODO: saveProgress' });
